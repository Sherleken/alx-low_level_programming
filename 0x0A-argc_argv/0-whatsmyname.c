#include <stdio.h>

/**
 * main - prints the name of program
 * @argc: number of command line arguments
 * @argv: name of array
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);

}
